#include "main.h"
/**
* mul - function to multiply two integer
*@a: integer to multiply
*@b: integer to multiply
* Return: 0 always
*/
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
